# 🚀 دليل البدء السريع | Quick Start Guide

## تشغيل المنصة بسهولة | Easy Platform Launch

### الطريقة الأولى: استخدام المنافذ الافتراضية الجديدة (موصى بها)

```bash
# 1. نسخ المشروع (إذا لم تقم بذلك بعد)
git clone https://github.com/malehhabool-hub/Offers_today_only.git
cd Offers_today_only

# 2. تثبيت المكتبات
npm install

# 3. تشغيل المنصة على المنافذ 4000 و 4001
./start-platform.sh
```

**الوصول للمنصة:**
- 🌐 واجهة المستخدم: http://localhost:4000
- 🔌 خادم API: http://localhost:4001

---

### الطريقة الثانية: استخدام منافذ مخصصة

```bash
# تشغيل على منافذ 5000 و 5001
./start-platform.sh 5000 5001

# تشغيل على منافذ 8080 و 8081
./start-platform.sh 8080 8081
```

**الوصول للمنصة:**
- 🌐 واجهة المستخدم: http://localhost:[FRONTEND_PORT]
- 🔌 خادم API: http://localhost:[BACKEND_PORT]

---

### الطريقة الثالثة: تحرير ملف .env

```bash
# 1. افتح ملف .env
nano .env

# 2. غيّر المنافذ حسب رغبتك
FRONTEND_PORT=6000
PORT=6001
ALLOWED_ORIGINS=http://localhost:6000
NEXT_PUBLIC_API_URL=http://localhost:6001

# 3. شغل المنصة
./start-platform.sh
```

---

## 🎯 أمثلة عملية | Practical Examples

### مثال 1: التشغيل على المنافذ 4000 و 4001
```bash
./start-platform.sh 4000 4001
# افتح http://localhost:4000
```

### مثال 2: التشغيل على المنافذ 8080 و 8081
```bash
./start-platform.sh 8080 8081
# افتح http://localhost:8080
```

### مثال 3: التشغيل اليدوي
```bash
# Terminal 1: تشغيل Backend على منفذ 7001
PORT=7001 npm run server

# Terminal 2: تشغيل Frontend على منفذ 7000
FRONTEND_PORT=7000 npm run dev

# افتح http://localhost:7000
```

---

## 🛑 إيقاف المنصة | Stop Platform

```bash
./stop-platform.sh
```

---

## ✅ التحقق من التشغيل | Verify Running

### اختبار Backend
```bash
curl http://localhost:4001/api/health
```

**النتيجة المتوقعة:**
```json
{"status":"ok","message":"Offers Design Platform API is running","timestamp":"..."}
```

### اختبار Frontend
```bash
curl -I http://localhost:4000
```

**النتيجة المتوقعة:**
```
HTTP/1.1 200 OK
```

---

## 🎨 استخدام المنصة | Using the Platform

بعد التشغيل، يمكنك:

1. **فتح المنصة في المتصفح**
   - اذهب إلى http://localhost:4000

2. **إنشاء عرض جديد**
   - اضغط على تبويب "مصمم العروض"
   - أدخل تفاصيل العرض
   - اختر الألوان
   - عاين التصميم
   - اضغط "تصدير"

3. **إدارة الوسائط**
   - اضغط على تبويب "مكتبة الوسائط"
   - ارفع صور أو فيديوهات
   - استخدمها في تصميماتك

---

## 🔧 حل المشاكل | Troubleshooting

### المنفذ مستخدم؟
```bash
# إيجاد العملية التي تستخدم المنفذ
lsof -i :4000

# إيقاف العملية
./stop-platform.sh

# أو استخدام منفذ آخر
./start-platform.sh 5000 5001
```

### Backend لا يعمل؟
```bash
# تحقق من أن Backend يعمل
ps aux | grep "node server"

# اختبر الاتصال
curl http://localhost:4001/api/health
```

### Frontend لا يعمل؟
```bash
# تحقق من أن Frontend يعمل
ps aux | grep "next dev"

# اختبر الاتصال
curl -I http://localhost:4000
```

---

## 📚 مزيد من المعلومات | More Information

- 📘 [دليل المنافذ المخصصة الكامل](CUSTOM_PORTS_GUIDE.md)
- 📖 [README الرئيسي](README.md)
- 🏗️ [معلومات البنية](ARCHITECTURE.md)

---

## 💡 نصائح | Tips

1. **استخدم منافذ فوق 1024** - لتجنب الحاجة لصلاحيات root
2. **استخدم منافذ متتالية** - مثل 4000/4001 للسهولة
3. **تحقق من المنافذ المتاحة** - قبل التشغيل
4. **استخدم السكربت** - أسهل من التشغيل اليدوي

---

**تم الإنشاء | Created:** 2025-11-07  
**الإصدار | Version:** 2.0.0  
**المنافذ الافتراضية | Default Ports:** 4000 (Frontend) & 4001 (Backend)
